# URL of the AVM backend (see /backend).
# In local dev, this points to the Express server on port 4000.
# In production (Surge), set this to your deployed Vercel backend URL, e.g.:
#   VITE_API_URL=https://your-backend.vercel.app
VITE_API_URL=http://localhost:4000
